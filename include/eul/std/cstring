#pragma GCC system_header

#include <stddef.h>
#include <string.h>

#pragma once

#undef strlen

extern "C++"
{
namespace std
{

std::size_t strlen(const char* str)
{
    return ::strlen(str);
}

} // namespace std
} // extern "C++"
